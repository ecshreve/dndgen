// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"github.com/ecshreve/dndgen/ent/class"
	"github.com/ecshreve/dndgen/ent/equipment"
	"github.com/ecshreve/dndgen/ent/predicate"
	"github.com/ecshreve/dndgen/ent/startingequipment"
)

// StartingEquipmentQuery is the builder for querying StartingEquipment entities.
type StartingEquipmentQuery struct {
	config
	ctx           *QueryContext
	order         []startingequipment.OrderOption
	inters        []Interceptor
	predicates    []predicate.StartingEquipment
	withClass     *ClassQuery
	withEquipment *EquipmentQuery
	modifiers     []func(*sql.Selector)
	loadTotal     []func(context.Context, []*StartingEquipment) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StartingEquipmentQuery builder.
func (seq *StartingEquipmentQuery) Where(ps ...predicate.StartingEquipment) *StartingEquipmentQuery {
	seq.predicates = append(seq.predicates, ps...)
	return seq
}

// Limit the number of records to be returned by this query.
func (seq *StartingEquipmentQuery) Limit(limit int) *StartingEquipmentQuery {
	seq.ctx.Limit = &limit
	return seq
}

// Offset to start from.
func (seq *StartingEquipmentQuery) Offset(offset int) *StartingEquipmentQuery {
	seq.ctx.Offset = &offset
	return seq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (seq *StartingEquipmentQuery) Unique(unique bool) *StartingEquipmentQuery {
	seq.ctx.Unique = &unique
	return seq
}

// Order specifies how the records should be ordered.
func (seq *StartingEquipmentQuery) Order(o ...startingequipment.OrderOption) *StartingEquipmentQuery {
	seq.order = append(seq.order, o...)
	return seq
}

// QueryClass chains the current query on the "class" edge.
func (seq *StartingEquipmentQuery) QueryClass() *ClassQuery {
	query := (&ClassClient{config: seq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := seq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := seq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(startingequipment.Table, startingequipment.ClassColumn, selector),
			sqlgraph.To(class.Table, class.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, startingequipment.ClassTable, startingequipment.ClassColumn),
		)
		fromU = sqlgraph.SetNeighbors(seq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEquipment chains the current query on the "equipment" edge.
func (seq *StartingEquipmentQuery) QueryEquipment() *EquipmentQuery {
	query := (&EquipmentClient{config: seq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := seq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := seq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(startingequipment.Table, startingequipment.EquipmentColumn, selector),
			sqlgraph.To(equipment.Table, equipment.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, startingequipment.EquipmentTable, startingequipment.EquipmentColumn),
		)
		fromU = sqlgraph.SetNeighbors(seq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first StartingEquipment entity from the query.
// Returns a *NotFoundError when no StartingEquipment was found.
func (seq *StartingEquipmentQuery) First(ctx context.Context) (*StartingEquipment, error) {
	nodes, err := seq.Limit(1).All(setContextOp(ctx, seq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{startingequipment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (seq *StartingEquipmentQuery) FirstX(ctx context.Context) *StartingEquipment {
	node, err := seq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// Only returns a single StartingEquipment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StartingEquipment entity is found.
// Returns a *NotFoundError when no StartingEquipment entities are found.
func (seq *StartingEquipmentQuery) Only(ctx context.Context) (*StartingEquipment, error) {
	nodes, err := seq.Limit(2).All(setContextOp(ctx, seq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{startingequipment.Label}
	default:
		return nil, &NotSingularError{startingequipment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (seq *StartingEquipmentQuery) OnlyX(ctx context.Context) *StartingEquipment {
	node, err := seq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// All executes the query and returns a list of StartingEquipments.
func (seq *StartingEquipmentQuery) All(ctx context.Context) ([]*StartingEquipment, error) {
	ctx = setContextOp(ctx, seq.ctx, "All")
	if err := seq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StartingEquipment, *StartingEquipmentQuery]()
	return withInterceptors[[]*StartingEquipment](ctx, seq, qr, seq.inters)
}

// AllX is like All, but panics if an error occurs.
func (seq *StartingEquipmentQuery) AllX(ctx context.Context) []*StartingEquipment {
	nodes, err := seq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// Count returns the count of the given query.
func (seq *StartingEquipmentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, seq.ctx, "Count")
	if err := seq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, seq, querierCount[*StartingEquipmentQuery](), seq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (seq *StartingEquipmentQuery) CountX(ctx context.Context) int {
	count, err := seq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (seq *StartingEquipmentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, seq.ctx, "Exist")
	switch _, err := seq.First(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (seq *StartingEquipmentQuery) ExistX(ctx context.Context) bool {
	exist, err := seq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StartingEquipmentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (seq *StartingEquipmentQuery) Clone() *StartingEquipmentQuery {
	if seq == nil {
		return nil
	}
	return &StartingEquipmentQuery{
		config:        seq.config,
		ctx:           seq.ctx.Clone(),
		order:         append([]startingequipment.OrderOption{}, seq.order...),
		inters:        append([]Interceptor{}, seq.inters...),
		predicates:    append([]predicate.StartingEquipment{}, seq.predicates...),
		withClass:     seq.withClass.Clone(),
		withEquipment: seq.withEquipment.Clone(),
		// clone intermediate query.
		sql:  seq.sql.Clone(),
		path: seq.path,
	}
}

// WithClass tells the query-builder to eager-load the nodes that are connected to
// the "class" edge. The optional arguments are used to configure the query builder of the edge.
func (seq *StartingEquipmentQuery) WithClass(opts ...func(*ClassQuery)) *StartingEquipmentQuery {
	query := (&ClassClient{config: seq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	seq.withClass = query
	return seq
}

// WithEquipment tells the query-builder to eager-load the nodes that are connected to
// the "equipment" edge. The optional arguments are used to configure the query builder of the edge.
func (seq *StartingEquipmentQuery) WithEquipment(opts ...func(*EquipmentQuery)) *StartingEquipmentQuery {
	query := (&EquipmentClient{config: seq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	seq.withEquipment = query
	return seq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ClassID int `json:"class_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StartingEquipment.Query().
//		GroupBy(startingequipment.FieldClassID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (seq *StartingEquipmentQuery) GroupBy(field string, fields ...string) *StartingEquipmentGroupBy {
	seq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StartingEquipmentGroupBy{build: seq}
	grbuild.flds = &seq.ctx.Fields
	grbuild.label = startingequipment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ClassID int `json:"class_id,omitempty"`
//	}
//
//	client.StartingEquipment.Query().
//		Select(startingequipment.FieldClassID).
//		Scan(ctx, &v)
func (seq *StartingEquipmentQuery) Select(fields ...string) *StartingEquipmentSelect {
	seq.ctx.Fields = append(seq.ctx.Fields, fields...)
	sbuild := &StartingEquipmentSelect{StartingEquipmentQuery: seq}
	sbuild.label = startingequipment.Label
	sbuild.flds, sbuild.scan = &seq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StartingEquipmentSelect configured with the given aggregations.
func (seq *StartingEquipmentQuery) Aggregate(fns ...AggregateFunc) *StartingEquipmentSelect {
	return seq.Select().Aggregate(fns...)
}

func (seq *StartingEquipmentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range seq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, seq); err != nil {
				return err
			}
		}
	}
	for _, f := range seq.ctx.Fields {
		if !startingequipment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if seq.path != nil {
		prev, err := seq.path(ctx)
		if err != nil {
			return err
		}
		seq.sql = prev
	}
	return nil
}

func (seq *StartingEquipmentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StartingEquipment, error) {
	var (
		nodes       = []*StartingEquipment{}
		_spec       = seq.querySpec()
		loadedTypes = [2]bool{
			seq.withClass != nil,
			seq.withEquipment != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StartingEquipment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StartingEquipment{config: seq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(seq.modifiers) > 0 {
		_spec.Modifiers = seq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, seq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := seq.withClass; query != nil {
		if err := seq.loadClass(ctx, query, nodes, nil,
			func(n *StartingEquipment, e *Class) { n.Edges.Class = e }); err != nil {
			return nil, err
		}
	}
	if query := seq.withEquipment; query != nil {
		if err := seq.loadEquipment(ctx, query, nodes, nil,
			func(n *StartingEquipment, e *Equipment) { n.Edges.Equipment = e }); err != nil {
			return nil, err
		}
	}
	for i := range seq.loadTotal {
		if err := seq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (seq *StartingEquipmentQuery) loadClass(ctx context.Context, query *ClassQuery, nodes []*StartingEquipment, init func(*StartingEquipment), assign func(*StartingEquipment, *Class)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*StartingEquipment)
	for i := range nodes {
		fk := nodes[i].ClassID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(class.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "class_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (seq *StartingEquipmentQuery) loadEquipment(ctx context.Context, query *EquipmentQuery, nodes []*StartingEquipment, init func(*StartingEquipment), assign func(*StartingEquipment, *Equipment)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*StartingEquipment)
	for i := range nodes {
		fk := nodes[i].EquipmentID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(equipment.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "equipment_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (seq *StartingEquipmentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := seq.querySpec()
	if len(seq.modifiers) > 0 {
		_spec.Modifiers = seq.modifiers
	}
	_spec.Unique = false
	_spec.Node.Columns = nil
	return sqlgraph.CountNodes(ctx, seq.driver, _spec)
}

func (seq *StartingEquipmentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(startingequipment.Table, startingequipment.Columns, nil)
	_spec.From = seq.sql
	if unique := seq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if seq.path != nil {
		_spec.Unique = true
	}
	if fields := seq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		for i := range fields {
			_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
		}
		if seq.withClass != nil {
			_spec.Node.AddColumnOnce(startingequipment.FieldClassID)
		}
		if seq.withEquipment != nil {
			_spec.Node.AddColumnOnce(startingequipment.FieldEquipmentID)
		}
	}
	if ps := seq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := seq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := seq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := seq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (seq *StartingEquipmentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(seq.driver.Dialect())
	t1 := builder.Table(startingequipment.Table)
	columns := seq.ctx.Fields
	if len(columns) == 0 {
		columns = startingequipment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if seq.sql != nil {
		selector = seq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if seq.ctx.Unique != nil && *seq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range seq.predicates {
		p(selector)
	}
	for _, p := range seq.order {
		p(selector)
	}
	if offset := seq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := seq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StartingEquipmentGroupBy is the group-by builder for StartingEquipment entities.
type StartingEquipmentGroupBy struct {
	selector
	build *StartingEquipmentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (segb *StartingEquipmentGroupBy) Aggregate(fns ...AggregateFunc) *StartingEquipmentGroupBy {
	segb.fns = append(segb.fns, fns...)
	return segb
}

// Scan applies the selector query and scans the result into the given value.
func (segb *StartingEquipmentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, segb.build.ctx, "GroupBy")
	if err := segb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StartingEquipmentQuery, *StartingEquipmentGroupBy](ctx, segb.build, segb, segb.build.inters, v)
}

func (segb *StartingEquipmentGroupBy) sqlScan(ctx context.Context, root *StartingEquipmentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(segb.fns))
	for _, fn := range segb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*segb.flds)+len(segb.fns))
		for _, f := range *segb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*segb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := segb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StartingEquipmentSelect is the builder for selecting fields of StartingEquipment entities.
type StartingEquipmentSelect struct {
	*StartingEquipmentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ses *StartingEquipmentSelect) Aggregate(fns ...AggregateFunc) *StartingEquipmentSelect {
	ses.fns = append(ses.fns, fns...)
	return ses
}

// Scan applies the selector query and scans the result into the given value.
func (ses *StartingEquipmentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ses.ctx, "Select")
	if err := ses.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StartingEquipmentQuery, *StartingEquipmentSelect](ctx, ses.StartingEquipmentQuery, ses, ses.inters, v)
}

func (ses *StartingEquipmentSelect) sqlScan(ctx context.Context, root *StartingEquipmentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ses.fns))
	for _, fn := range ses.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ses.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ses.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
