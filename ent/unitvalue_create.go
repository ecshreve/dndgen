// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ecshreve/dndgen/ent/unitvalue"
)

// UnitValueCreate is the builder for creating a UnitValue entity.
type UnitValueCreate struct {
	config
	mutation *UnitValueMutation
	hooks    []Hook
}

// SetVal sets the "val" field.
func (uvc *UnitValueCreate) SetVal(i int) *UnitValueCreate {
	uvc.mutation.SetVal(i)
	return uvc
}

// SetUnit sets the "unit" field.
func (uvc *UnitValueCreate) SetUnit(s string) *UnitValueCreate {
	uvc.mutation.SetUnit(s)
	return uvc
}

// Mutation returns the UnitValueMutation object of the builder.
func (uvc *UnitValueCreate) Mutation() *UnitValueMutation {
	return uvc.mutation
}

// Save creates the UnitValue in the database.
func (uvc *UnitValueCreate) Save(ctx context.Context) (*UnitValue, error) {
	return withHooks[*UnitValue, UnitValueMutation](ctx, uvc.sqlSave, uvc.mutation, uvc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (uvc *UnitValueCreate) SaveX(ctx context.Context) *UnitValue {
	v, err := uvc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uvc *UnitValueCreate) Exec(ctx context.Context) error {
	_, err := uvc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uvc *UnitValueCreate) ExecX(ctx context.Context) {
	if err := uvc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (uvc *UnitValueCreate) check() error {
	if _, ok := uvc.mutation.Val(); !ok {
		return &ValidationError{Name: "val", err: errors.New(`ent: missing required field "UnitValue.val"`)}
	}
	if _, ok := uvc.mutation.Unit(); !ok {
		return &ValidationError{Name: "unit", err: errors.New(`ent: missing required field "UnitValue.unit"`)}
	}
	return nil
}

func (uvc *UnitValueCreate) sqlSave(ctx context.Context) (*UnitValue, error) {
	if err := uvc.check(); err != nil {
		return nil, err
	}
	_node, _spec := uvc.createSpec()
	if err := sqlgraph.CreateNode(ctx, uvc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	uvc.mutation.id = &_node.ID
	uvc.mutation.done = true
	return _node, nil
}

func (uvc *UnitValueCreate) createSpec() (*UnitValue, *sqlgraph.CreateSpec) {
	var (
		_node = &UnitValue{config: uvc.config}
		_spec = sqlgraph.NewCreateSpec(unitvalue.Table, sqlgraph.NewFieldSpec(unitvalue.FieldID, field.TypeInt))
	)
	if value, ok := uvc.mutation.Val(); ok {
		_spec.SetField(unitvalue.FieldVal, field.TypeInt, value)
		_node.Val = value
	}
	if value, ok := uvc.mutation.Unit(); ok {
		_spec.SetField(unitvalue.FieldUnit, field.TypeString, value)
		_node.Unit = value
	}
	return _node, _spec
}

// UnitValueCreateBulk is the builder for creating many UnitValue entities in bulk.
type UnitValueCreateBulk struct {
	config
	builders []*UnitValueCreate
}

// Save creates the UnitValue entities in the database.
func (uvcb *UnitValueCreateBulk) Save(ctx context.Context) ([]*UnitValue, error) {
	specs := make([]*sqlgraph.CreateSpec, len(uvcb.builders))
	nodes := make([]*UnitValue, len(uvcb.builders))
	mutators := make([]Mutator, len(uvcb.builders))
	for i := range uvcb.builders {
		func(i int, root context.Context) {
			builder := uvcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*UnitValueMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, uvcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, uvcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, uvcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (uvcb *UnitValueCreateBulk) SaveX(ctx context.Context) []*UnitValue {
	v, err := uvcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uvcb *UnitValueCreateBulk) Exec(ctx context.Context) error {
	_, err := uvcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uvcb *UnitValueCreateBulk) ExecX(ctx context.Context) {
	if err := uvcb.Exec(ctx); err != nil {
		panic(err)
	}
}
