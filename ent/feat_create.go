// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ecshreve/dndgen/ent/feat"
)

// FeatCreate is the builder for creating a Feat entity.
type FeatCreate struct {
	config
	mutation *FeatMutation
	hooks    []Hook
}

// SetIndx sets the "indx" field.
func (fc *FeatCreate) SetIndx(s string) *FeatCreate {
	fc.mutation.SetIndx(s)
	return fc
}

// SetName sets the "name" field.
func (fc *FeatCreate) SetName(s string) *FeatCreate {
	fc.mutation.SetName(s)
	return fc
}

// SetDesc sets the "desc" field.
func (fc *FeatCreate) SetDesc(s []string) *FeatCreate {
	fc.mutation.SetDesc(s)
	return fc
}

// Mutation returns the FeatMutation object of the builder.
func (fc *FeatCreate) Mutation() *FeatMutation {
	return fc.mutation
}

// Save creates the Feat in the database.
func (fc *FeatCreate) Save(ctx context.Context) (*Feat, error) {
	return withHooks(ctx, fc.sqlSave, fc.mutation, fc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (fc *FeatCreate) SaveX(ctx context.Context) *Feat {
	v, err := fc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fc *FeatCreate) Exec(ctx context.Context) error {
	_, err := fc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fc *FeatCreate) ExecX(ctx context.Context) {
	if err := fc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (fc *FeatCreate) check() error {
	if _, ok := fc.mutation.Indx(); !ok {
		return &ValidationError{Name: "indx", err: errors.New(`ent: missing required field "Feat.indx"`)}
	}
	if v, ok := fc.mutation.Indx(); ok {
		if err := feat.IndxValidator(v); err != nil {
			return &ValidationError{Name: "indx", err: fmt.Errorf(`ent: validator failed for field "Feat.indx": %w`, err)}
		}
	}
	if _, ok := fc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "Feat.name"`)}
	}
	if v, ok := fc.mutation.Name(); ok {
		if err := feat.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf(`ent: validator failed for field "Feat.name": %w`, err)}
		}
	}
	return nil
}

func (fc *FeatCreate) sqlSave(ctx context.Context) (*Feat, error) {
	if err := fc.check(); err != nil {
		return nil, err
	}
	_node, _spec := fc.createSpec()
	if err := sqlgraph.CreateNode(ctx, fc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	fc.mutation.id = &_node.ID
	fc.mutation.done = true
	return _node, nil
}

func (fc *FeatCreate) createSpec() (*Feat, *sqlgraph.CreateSpec) {
	var (
		_node = &Feat{config: fc.config}
		_spec = sqlgraph.NewCreateSpec(feat.Table, sqlgraph.NewFieldSpec(feat.FieldID, field.TypeInt))
	)
	if value, ok := fc.mutation.Indx(); ok {
		_spec.SetField(feat.FieldIndx, field.TypeString, value)
		_node.Indx = value
	}
	if value, ok := fc.mutation.Name(); ok {
		_spec.SetField(feat.FieldName, field.TypeString, value)
		_node.Name = value
	}
	if value, ok := fc.mutation.Desc(); ok {
		_spec.SetField(feat.FieldDesc, field.TypeJSON, value)
		_node.Desc = value
	}
	return _node, _spec
}

// FeatCreateBulk is the builder for creating many Feat entities in bulk.
type FeatCreateBulk struct {
	config
	err      error
	builders []*FeatCreate
}

// Save creates the Feat entities in the database.
func (fcb *FeatCreateBulk) Save(ctx context.Context) ([]*Feat, error) {
	if fcb.err != nil {
		return nil, fcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(fcb.builders))
	nodes := make([]*Feat, len(fcb.builders))
	mutators := make([]Mutator, len(fcb.builders))
	for i := range fcb.builders {
		func(i int, root context.Context) {
			builder := fcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*FeatMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, fcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, fcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, fcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (fcb *FeatCreateBulk) SaveX(ctx context.Context) []*Feat {
	v, err := fcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (fcb *FeatCreateBulk) Exec(ctx context.Context) error {
	_, err := fcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (fcb *FeatCreateBulk) ExecX(ctx context.Context) {
	if err := fcb.Exec(ctx); err != nil {
		panic(err)
	}
}
