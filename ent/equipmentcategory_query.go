// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ecshreve/dndgen/ent/equipment"
	"github.com/ecshreve/dndgen/ent/equipmentcategory"
	"github.com/ecshreve/dndgen/ent/predicate"
)

// EquipmentCategoryQuery is the builder for querying EquipmentCategory entities.
type EquipmentCategoryQuery struct {
	config
	ctx                *QueryContext
	order              []equipmentcategory.OrderOption
	inters             []Interceptor
	predicates         []predicate.EquipmentCategory
	withParent         *EquipmentCategoryQuery
	withChildren       *EquipmentCategoryQuery
	withEquipment      *EquipmentQuery
	withFKs            bool
	modifiers          []func(*sql.Selector)
	loadTotal          []func(context.Context, []*EquipmentCategory) error
	withNamedChildren  map[string]*EquipmentCategoryQuery
	withNamedEquipment map[string]*EquipmentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EquipmentCategoryQuery builder.
func (ecq *EquipmentCategoryQuery) Where(ps ...predicate.EquipmentCategory) *EquipmentCategoryQuery {
	ecq.predicates = append(ecq.predicates, ps...)
	return ecq
}

// Limit the number of records to be returned by this query.
func (ecq *EquipmentCategoryQuery) Limit(limit int) *EquipmentCategoryQuery {
	ecq.ctx.Limit = &limit
	return ecq
}

// Offset to start from.
func (ecq *EquipmentCategoryQuery) Offset(offset int) *EquipmentCategoryQuery {
	ecq.ctx.Offset = &offset
	return ecq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ecq *EquipmentCategoryQuery) Unique(unique bool) *EquipmentCategoryQuery {
	ecq.ctx.Unique = &unique
	return ecq
}

// Order specifies how the records should be ordered.
func (ecq *EquipmentCategoryQuery) Order(o ...equipmentcategory.OrderOption) *EquipmentCategoryQuery {
	ecq.order = append(ecq.order, o...)
	return ecq
}

// QueryParent chains the current query on the "parent" edge.
func (ecq *EquipmentCategoryQuery) QueryParent() *EquipmentCategoryQuery {
	query := (&EquipmentCategoryClient{config: ecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(equipmentcategory.Table, equipmentcategory.FieldID, selector),
			sqlgraph.To(equipmentcategory.Table, equipmentcategory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, equipmentcategory.ParentTable, equipmentcategory.ParentColumn),
		)
		fromU = sqlgraph.SetNeighbors(ecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryChildren chains the current query on the "children" edge.
func (ecq *EquipmentCategoryQuery) QueryChildren() *EquipmentCategoryQuery {
	query := (&EquipmentCategoryClient{config: ecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(equipmentcategory.Table, equipmentcategory.FieldID, selector),
			sqlgraph.To(equipmentcategory.Table, equipmentcategory.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, equipmentcategory.ChildrenTable, equipmentcategory.ChildrenColumn),
		)
		fromU = sqlgraph.SetNeighbors(ecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEquipment chains the current query on the "equipment" edge.
func (ecq *EquipmentCategoryQuery) QueryEquipment() *EquipmentQuery {
	query := (&EquipmentClient{config: ecq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ecq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ecq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(equipmentcategory.Table, equipmentcategory.FieldID, selector),
			sqlgraph.To(equipment.Table, equipment.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, equipmentcategory.EquipmentTable, equipmentcategory.EquipmentPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(ecq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EquipmentCategory entity from the query.
// Returns a *NotFoundError when no EquipmentCategory was found.
func (ecq *EquipmentCategoryQuery) First(ctx context.Context) (*EquipmentCategory, error) {
	nodes, err := ecq.Limit(1).All(setContextOp(ctx, ecq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{equipmentcategory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) FirstX(ctx context.Context) *EquipmentCategory {
	node, err := ecq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EquipmentCategory ID from the query.
// Returns a *NotFoundError when no EquipmentCategory ID was found.
func (ecq *EquipmentCategoryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ecq.Limit(1).IDs(setContextOp(ctx, ecq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{equipmentcategory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) FirstIDX(ctx context.Context) int {
	id, err := ecq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EquipmentCategory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EquipmentCategory entity is found.
// Returns a *NotFoundError when no EquipmentCategory entities are found.
func (ecq *EquipmentCategoryQuery) Only(ctx context.Context) (*EquipmentCategory, error) {
	nodes, err := ecq.Limit(2).All(setContextOp(ctx, ecq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{equipmentcategory.Label}
	default:
		return nil, &NotSingularError{equipmentcategory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) OnlyX(ctx context.Context) *EquipmentCategory {
	node, err := ecq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EquipmentCategory ID in the query.
// Returns a *NotSingularError when more than one EquipmentCategory ID is found.
// Returns a *NotFoundError when no entities are found.
func (ecq *EquipmentCategoryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ecq.Limit(2).IDs(setContextOp(ctx, ecq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{equipmentcategory.Label}
	default:
		err = &NotSingularError{equipmentcategory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) OnlyIDX(ctx context.Context) int {
	id, err := ecq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EquipmentCategories.
func (ecq *EquipmentCategoryQuery) All(ctx context.Context) ([]*EquipmentCategory, error) {
	ctx = setContextOp(ctx, ecq.ctx, "All")
	if err := ecq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EquipmentCategory, *EquipmentCategoryQuery]()
	return withInterceptors[[]*EquipmentCategory](ctx, ecq, qr, ecq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) AllX(ctx context.Context) []*EquipmentCategory {
	nodes, err := ecq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EquipmentCategory IDs.
func (ecq *EquipmentCategoryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ecq.ctx.Unique == nil && ecq.path != nil {
		ecq.Unique(true)
	}
	ctx = setContextOp(ctx, ecq.ctx, "IDs")
	if err = ecq.Select(equipmentcategory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) IDsX(ctx context.Context) []int {
	ids, err := ecq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ecq *EquipmentCategoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ecq.ctx, "Count")
	if err := ecq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ecq, querierCount[*EquipmentCategoryQuery](), ecq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) CountX(ctx context.Context) int {
	count, err := ecq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ecq *EquipmentCategoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ecq.ctx, "Exist")
	switch _, err := ecq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ecq *EquipmentCategoryQuery) ExistX(ctx context.Context) bool {
	exist, err := ecq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EquipmentCategoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ecq *EquipmentCategoryQuery) Clone() *EquipmentCategoryQuery {
	if ecq == nil {
		return nil
	}
	return &EquipmentCategoryQuery{
		config:        ecq.config,
		ctx:           ecq.ctx.Clone(),
		order:         append([]equipmentcategory.OrderOption{}, ecq.order...),
		inters:        append([]Interceptor{}, ecq.inters...),
		predicates:    append([]predicate.EquipmentCategory{}, ecq.predicates...),
		withParent:    ecq.withParent.Clone(),
		withChildren:  ecq.withChildren.Clone(),
		withEquipment: ecq.withEquipment.Clone(),
		// clone intermediate query.
		sql:  ecq.sql.Clone(),
		path: ecq.path,
	}
}

// WithParent tells the query-builder to eager-load the nodes that are connected to
// the "parent" edge. The optional arguments are used to configure the query builder of the edge.
func (ecq *EquipmentCategoryQuery) WithParent(opts ...func(*EquipmentCategoryQuery)) *EquipmentCategoryQuery {
	query := (&EquipmentCategoryClient{config: ecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ecq.withParent = query
	return ecq
}

// WithChildren tells the query-builder to eager-load the nodes that are connected to
// the "children" edge. The optional arguments are used to configure the query builder of the edge.
func (ecq *EquipmentCategoryQuery) WithChildren(opts ...func(*EquipmentCategoryQuery)) *EquipmentCategoryQuery {
	query := (&EquipmentCategoryClient{config: ecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ecq.withChildren = query
	return ecq
}

// WithEquipment tells the query-builder to eager-load the nodes that are connected to
// the "equipment" edge. The optional arguments are used to configure the query builder of the edge.
func (ecq *EquipmentCategoryQuery) WithEquipment(opts ...func(*EquipmentQuery)) *EquipmentCategoryQuery {
	query := (&EquipmentClient{config: ecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ecq.withEquipment = query
	return ecq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ParentCategoryID int `json:"parent_category_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EquipmentCategory.Query().
//		GroupBy(equipmentcategory.FieldParentCategoryID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ecq *EquipmentCategoryQuery) GroupBy(field string, fields ...string) *EquipmentCategoryGroupBy {
	ecq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EquipmentCategoryGroupBy{build: ecq}
	grbuild.flds = &ecq.ctx.Fields
	grbuild.label = equipmentcategory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ParentCategoryID int `json:"parent_category_id,omitempty"`
//	}
//
//	client.EquipmentCategory.Query().
//		Select(equipmentcategory.FieldParentCategoryID).
//		Scan(ctx, &v)
func (ecq *EquipmentCategoryQuery) Select(fields ...string) *EquipmentCategorySelect {
	ecq.ctx.Fields = append(ecq.ctx.Fields, fields...)
	sbuild := &EquipmentCategorySelect{EquipmentCategoryQuery: ecq}
	sbuild.label = equipmentcategory.Label
	sbuild.flds, sbuild.scan = &ecq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EquipmentCategorySelect configured with the given aggregations.
func (ecq *EquipmentCategoryQuery) Aggregate(fns ...AggregateFunc) *EquipmentCategorySelect {
	return ecq.Select().Aggregate(fns...)
}

func (ecq *EquipmentCategoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ecq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ecq); err != nil {
				return err
			}
		}
	}
	for _, f := range ecq.ctx.Fields {
		if !equipmentcategory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ecq.path != nil {
		prev, err := ecq.path(ctx)
		if err != nil {
			return err
		}
		ecq.sql = prev
	}
	return nil
}

func (ecq *EquipmentCategoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EquipmentCategory, error) {
	var (
		nodes       = []*EquipmentCategory{}
		withFKs     = ecq.withFKs
		_spec       = ecq.querySpec()
		loadedTypes = [3]bool{
			ecq.withParent != nil,
			ecq.withChildren != nil,
			ecq.withEquipment != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, equipmentcategory.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EquipmentCategory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EquipmentCategory{config: ecq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ecq.modifiers) > 0 {
		_spec.Modifiers = ecq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ecq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ecq.withParent; query != nil {
		if err := ecq.loadParent(ctx, query, nodes, nil,
			func(n *EquipmentCategory, e *EquipmentCategory) { n.Edges.Parent = e }); err != nil {
			return nil, err
		}
	}
	if query := ecq.withChildren; query != nil {
		if err := ecq.loadChildren(ctx, query, nodes,
			func(n *EquipmentCategory) { n.Edges.Children = []*EquipmentCategory{} },
			func(n *EquipmentCategory, e *EquipmentCategory) { n.Edges.Children = append(n.Edges.Children, e) }); err != nil {
			return nil, err
		}
	}
	if query := ecq.withEquipment; query != nil {
		if err := ecq.loadEquipment(ctx, query, nodes,
			func(n *EquipmentCategory) { n.Edges.Equipment = []*Equipment{} },
			func(n *EquipmentCategory, e *Equipment) { n.Edges.Equipment = append(n.Edges.Equipment, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range ecq.withNamedChildren {
		if err := ecq.loadChildren(ctx, query, nodes,
			func(n *EquipmentCategory) { n.appendNamedChildren(name) },
			func(n *EquipmentCategory, e *EquipmentCategory) { n.appendNamedChildren(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range ecq.withNamedEquipment {
		if err := ecq.loadEquipment(ctx, query, nodes,
			func(n *EquipmentCategory) { n.appendNamedEquipment(name) },
			func(n *EquipmentCategory, e *Equipment) { n.appendNamedEquipment(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range ecq.loadTotal {
		if err := ecq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ecq *EquipmentCategoryQuery) loadParent(ctx context.Context, query *EquipmentCategoryQuery, nodes []*EquipmentCategory, init func(*EquipmentCategory), assign func(*EquipmentCategory, *EquipmentCategory)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*EquipmentCategory)
	for i := range nodes {
		fk := nodes[i].ParentCategoryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(equipmentcategory.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "parent_category_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (ecq *EquipmentCategoryQuery) loadChildren(ctx context.Context, query *EquipmentCategoryQuery, nodes []*EquipmentCategory, init func(*EquipmentCategory), assign func(*EquipmentCategory, *EquipmentCategory)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*EquipmentCategory)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(equipmentcategory.FieldParentCategoryID)
	}
	query.Where(predicate.EquipmentCategory(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(equipmentcategory.ChildrenColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.ParentCategoryID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "parent_category_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (ecq *EquipmentCategoryQuery) loadEquipment(ctx context.Context, query *EquipmentQuery, nodes []*EquipmentCategory, init func(*EquipmentCategory), assign func(*EquipmentCategory, *Equipment)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[int]*EquipmentCategory)
	nids := make(map[int]map[*EquipmentCategory]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(equipmentcategory.EquipmentTable)
		s.Join(joinT).On(s.C(equipment.FieldID), joinT.C(equipmentcategory.EquipmentPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(equipmentcategory.EquipmentPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(equipmentcategory.EquipmentPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(sql.NullInt64)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := int(values[0].(*sql.NullInt64).Int64)
				inValue := int(values[1].(*sql.NullInt64).Int64)
				if nids[inValue] == nil {
					nids[inValue] = map[*EquipmentCategory]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*Equipment](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "equipment" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}

func (ecq *EquipmentCategoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ecq.querySpec()
	if len(ecq.modifiers) > 0 {
		_spec.Modifiers = ecq.modifiers
	}
	_spec.Node.Columns = ecq.ctx.Fields
	if len(ecq.ctx.Fields) > 0 {
		_spec.Unique = ecq.ctx.Unique != nil && *ecq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ecq.driver, _spec)
}

func (ecq *EquipmentCategoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(equipmentcategory.Table, equipmentcategory.Columns, sqlgraph.NewFieldSpec(equipmentcategory.FieldID, field.TypeInt))
	_spec.From = ecq.sql
	if unique := ecq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ecq.path != nil {
		_spec.Unique = true
	}
	if fields := ecq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, equipmentcategory.FieldID)
		for i := range fields {
			if fields[i] != equipmentcategory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if ecq.withParent != nil {
			_spec.Node.AddColumnOnce(equipmentcategory.FieldParentCategoryID)
		}
	}
	if ps := ecq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ecq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ecq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ecq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ecq *EquipmentCategoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ecq.driver.Dialect())
	t1 := builder.Table(equipmentcategory.Table)
	columns := ecq.ctx.Fields
	if len(columns) == 0 {
		columns = equipmentcategory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ecq.sql != nil {
		selector = ecq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ecq.ctx.Unique != nil && *ecq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ecq.predicates {
		p(selector)
	}
	for _, p := range ecq.order {
		p(selector)
	}
	if offset := ecq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ecq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedChildren tells the query-builder to eager-load the nodes that are connected to the "children"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (ecq *EquipmentCategoryQuery) WithNamedChildren(name string, opts ...func(*EquipmentCategoryQuery)) *EquipmentCategoryQuery {
	query := (&EquipmentCategoryClient{config: ecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if ecq.withNamedChildren == nil {
		ecq.withNamedChildren = make(map[string]*EquipmentCategoryQuery)
	}
	ecq.withNamedChildren[name] = query
	return ecq
}

// WithNamedEquipment tells the query-builder to eager-load the nodes that are connected to the "equipment"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (ecq *EquipmentCategoryQuery) WithNamedEquipment(name string, opts ...func(*EquipmentQuery)) *EquipmentCategoryQuery {
	query := (&EquipmentClient{config: ecq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if ecq.withNamedEquipment == nil {
		ecq.withNamedEquipment = make(map[string]*EquipmentQuery)
	}
	ecq.withNamedEquipment[name] = query
	return ecq
}

// EquipmentCategoryGroupBy is the group-by builder for EquipmentCategory entities.
type EquipmentCategoryGroupBy struct {
	selector
	build *EquipmentCategoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ecgb *EquipmentCategoryGroupBy) Aggregate(fns ...AggregateFunc) *EquipmentCategoryGroupBy {
	ecgb.fns = append(ecgb.fns, fns...)
	return ecgb
}

// Scan applies the selector query and scans the result into the given value.
func (ecgb *EquipmentCategoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ecgb.build.ctx, "GroupBy")
	if err := ecgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EquipmentCategoryQuery, *EquipmentCategoryGroupBy](ctx, ecgb.build, ecgb, ecgb.build.inters, v)
}

func (ecgb *EquipmentCategoryGroupBy) sqlScan(ctx context.Context, root *EquipmentCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ecgb.fns))
	for _, fn := range ecgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ecgb.flds)+len(ecgb.fns))
		for _, f := range *ecgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ecgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ecgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EquipmentCategorySelect is the builder for selecting fields of EquipmentCategory entities.
type EquipmentCategorySelect struct {
	*EquipmentCategoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ecs *EquipmentCategorySelect) Aggregate(fns ...AggregateFunc) *EquipmentCategorySelect {
	ecs.fns = append(ecs.fns, fns...)
	return ecs
}

// Scan applies the selector query and scans the result into the given value.
func (ecs *EquipmentCategorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ecs.ctx, "Select")
	if err := ecs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EquipmentCategoryQuery, *EquipmentCategorySelect](ctx, ecs.EquipmentCategoryQuery, ecs, ecs.inters, v)
}

func (ecs *EquipmentCategorySelect) sqlScan(ctx context.Context, root *EquipmentCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ecs.fns))
	for _, fn := range ecs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ecs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ecs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
