// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ecshreve/dndgen/ent/class"
	"github.com/ecshreve/dndgen/ent/equipment"
	"github.com/ecshreve/dndgen/ent/startingequipment"
)

// StartingEquipmentCreate is the builder for creating a StartingEquipment entity.
type StartingEquipmentCreate struct {
	config
	mutation *StartingEquipmentMutation
	hooks    []Hook
}

// SetClassID sets the "class_id" field.
func (sec *StartingEquipmentCreate) SetClassID(i int) *StartingEquipmentCreate {
	sec.mutation.SetClassID(i)
	return sec
}

// SetEquipmentID sets the "equipment_id" field.
func (sec *StartingEquipmentCreate) SetEquipmentID(i int) *StartingEquipmentCreate {
	sec.mutation.SetEquipmentID(i)
	return sec
}

// SetQuantity sets the "quantity" field.
func (sec *StartingEquipmentCreate) SetQuantity(i int) *StartingEquipmentCreate {
	sec.mutation.SetQuantity(i)
	return sec
}

// SetClass sets the "class" edge to the Class entity.
func (sec *StartingEquipmentCreate) SetClass(c *Class) *StartingEquipmentCreate {
	return sec.SetClassID(c.ID)
}

// SetEquipment sets the "equipment" edge to the Equipment entity.
func (sec *StartingEquipmentCreate) SetEquipment(e *Equipment) *StartingEquipmentCreate {
	return sec.SetEquipmentID(e.ID)
}

// Mutation returns the StartingEquipmentMutation object of the builder.
func (sec *StartingEquipmentCreate) Mutation() *StartingEquipmentMutation {
	return sec.mutation
}

// Save creates the StartingEquipment in the database.
func (sec *StartingEquipmentCreate) Save(ctx context.Context) (*StartingEquipment, error) {
	return withHooks(ctx, sec.sqlSave, sec.mutation, sec.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (sec *StartingEquipmentCreate) SaveX(ctx context.Context) *StartingEquipment {
	v, err := sec.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sec *StartingEquipmentCreate) Exec(ctx context.Context) error {
	_, err := sec.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sec *StartingEquipmentCreate) ExecX(ctx context.Context) {
	if err := sec.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sec *StartingEquipmentCreate) check() error {
	if _, ok := sec.mutation.ClassID(); !ok {
		return &ValidationError{Name: "class_id", err: errors.New(`ent: missing required field "StartingEquipment.class_id"`)}
	}
	if _, ok := sec.mutation.EquipmentID(); !ok {
		return &ValidationError{Name: "equipment_id", err: errors.New(`ent: missing required field "StartingEquipment.equipment_id"`)}
	}
	if _, ok := sec.mutation.Quantity(); !ok {
		return &ValidationError{Name: "quantity", err: errors.New(`ent: missing required field "StartingEquipment.quantity"`)}
	}
	if _, ok := sec.mutation.ClassID(); !ok {
		return &ValidationError{Name: "class", err: errors.New(`ent: missing required edge "StartingEquipment.class"`)}
	}
	if _, ok := sec.mutation.EquipmentID(); !ok {
		return &ValidationError{Name: "equipment", err: errors.New(`ent: missing required edge "StartingEquipment.equipment"`)}
	}
	return nil
}

func (sec *StartingEquipmentCreate) sqlSave(ctx context.Context) (*StartingEquipment, error) {
	if err := sec.check(); err != nil {
		return nil, err
	}
	_node, _spec := sec.createSpec()
	if err := sqlgraph.CreateNode(ctx, sec.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	return _node, nil
}

func (sec *StartingEquipmentCreate) createSpec() (*StartingEquipment, *sqlgraph.CreateSpec) {
	var (
		_node = &StartingEquipment{config: sec.config}
		_spec = sqlgraph.NewCreateSpec(startingequipment.Table, nil)
	)
	if value, ok := sec.mutation.Quantity(); ok {
		_spec.SetField(startingequipment.FieldQuantity, field.TypeInt, value)
		_node.Quantity = value
	}
	if nodes := sec.mutation.ClassIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   startingequipment.ClassTable,
			Columns: []string{startingequipment.ClassColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(class.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ClassID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := sec.mutation.EquipmentIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   startingequipment.EquipmentTable,
			Columns: []string{startingequipment.EquipmentColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(equipment.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.EquipmentID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// StartingEquipmentCreateBulk is the builder for creating many StartingEquipment entities in bulk.
type StartingEquipmentCreateBulk struct {
	config
	builders []*StartingEquipmentCreate
}

// Save creates the StartingEquipment entities in the database.
func (secb *StartingEquipmentCreateBulk) Save(ctx context.Context) ([]*StartingEquipment, error) {
	specs := make([]*sqlgraph.CreateSpec, len(secb.builders))
	nodes := make([]*StartingEquipment, len(secb.builders))
	mutators := make([]Mutator, len(secb.builders))
	for i := range secb.builders {
		func(i int, root context.Context) {
			builder := secb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*StartingEquipmentMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, secb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, secb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, secb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (secb *StartingEquipmentCreateBulk) SaveX(ctx context.Context) []*StartingEquipment {
	v, err := secb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (secb *StartingEquipmentCreateBulk) Exec(ctx context.Context) error {
	_, err := secb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (secb *StartingEquipmentCreateBulk) ExecX(ctx context.Context) {
	if err := secb.Exec(ctx); err != nil {
		panic(err)
	}
}
