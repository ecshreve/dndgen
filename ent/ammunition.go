// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ecshreve/dndgen/ent/ammunition"
)

// Ammunition is the model entity for the Ammunition schema.
type Ammunition struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity int `json:"quantity,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AmmunitionQuery when eager-loading is set.
	Edges        AmmunitionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AmmunitionEdges holds the relations/edges for other nodes in the graph.
type AmmunitionEdges struct {
	// Equipment holds the value of the equipment edge.
	Equipment []*Equipment `json:"equipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedEquipment map[string][]*Equipment
}

// EquipmentOrErr returns the Equipment value or an error if the edge
// was not loaded in eager-loading.
func (e AmmunitionEdges) EquipmentOrErr() ([]*Equipment, error) {
	if e.loadedTypes[0] {
		return e.Equipment, nil
	}
	return nil, &NotLoadedError{edge: "equipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Ammunition) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ammunition.FieldID, ammunition.FieldQuantity:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Ammunition fields.
func (a *Ammunition) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ammunition.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			a.ID = int(value.Int64)
		case ammunition.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				a.Quantity = int(value.Int64)
			}
		default:
			a.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Ammunition.
// This includes values selected through modifiers, order, etc.
func (a *Ammunition) Value(name string) (ent.Value, error) {
	return a.selectValues.Get(name)
}

// QueryEquipment queries the "equipment" edge of the Ammunition entity.
func (a *Ammunition) QueryEquipment() *EquipmentQuery {
	return NewAmmunitionClient(a.config).QueryEquipment(a)
}

// Update returns a builder for updating this Ammunition.
// Note that you need to call Ammunition.Unwrap() before calling this method if this Ammunition
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Ammunition) Update() *AmmunitionUpdateOne {
	return NewAmmunitionClient(a.config).UpdateOne(a)
}

// Unwrap unwraps the Ammunition entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Ammunition) Unwrap() *Ammunition {
	_tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Ammunition is not a transactional entity")
	}
	a.config.driver = _tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Ammunition) String() string {
	var builder strings.Builder
	builder.WriteString("Ammunition(")
	builder.WriteString(fmt.Sprintf("id=%v, ", a.ID))
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", a.Quantity))
	builder.WriteByte(')')
	return builder.String()
}

func (ac *AmmunitionCreate) SetAmmunition(input *Ammunition) *AmmunitionCreate {
	ac.SetQuantity(input.Quantity)
	return ac
}

// NamedEquipment returns the Equipment named value or an error if the edge was not
// loaded in eager-loading with this name.
func (a *Ammunition) NamedEquipment(name string) ([]*Equipment, error) {
	if a.Edges.namedEquipment == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := a.Edges.namedEquipment[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (a *Ammunition) appendNamedEquipment(name string, edges ...*Equipment) {
	if a.Edges.namedEquipment == nil {
		a.Edges.namedEquipment = make(map[string][]*Equipment)
	}
	if len(edges) == 0 {
		a.Edges.namedEquipment[name] = []*Equipment{}
	} else {
		a.Edges.namedEquipment[name] = append(a.Edges.namedEquipment[name], edges...)
	}
}

// Ammunitions is a parsable slice of Ammunition.
type Ammunitions []*Ammunition
