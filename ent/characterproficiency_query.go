// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ecshreve/dndgen/ent/character"
	"github.com/ecshreve/dndgen/ent/characterproficiency"
	"github.com/ecshreve/dndgen/ent/characterskill"
	"github.com/ecshreve/dndgen/ent/predicate"
	"github.com/ecshreve/dndgen/ent/proficiency"
)

// CharacterProficiencyQuery is the builder for querying CharacterProficiency entities.
type CharacterProficiencyQuery struct {
	config
	ctx                *QueryContext
	order              []characterproficiency.OrderOption
	inters             []Interceptor
	predicates         []predicate.CharacterProficiency
	withCharacter      *CharacterQuery
	withProficiency    *ProficiencyQuery
	withCharacterSkill *CharacterSkillQuery
	withFKs            bool
	modifiers          []func(*sql.Selector)
	loadTotal          []func(context.Context, []*CharacterProficiency) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CharacterProficiencyQuery builder.
func (cpq *CharacterProficiencyQuery) Where(ps ...predicate.CharacterProficiency) *CharacterProficiencyQuery {
	cpq.predicates = append(cpq.predicates, ps...)
	return cpq
}

// Limit the number of records to be returned by this query.
func (cpq *CharacterProficiencyQuery) Limit(limit int) *CharacterProficiencyQuery {
	cpq.ctx.Limit = &limit
	return cpq
}

// Offset to start from.
func (cpq *CharacterProficiencyQuery) Offset(offset int) *CharacterProficiencyQuery {
	cpq.ctx.Offset = &offset
	return cpq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cpq *CharacterProficiencyQuery) Unique(unique bool) *CharacterProficiencyQuery {
	cpq.ctx.Unique = &unique
	return cpq
}

// Order specifies how the records should be ordered.
func (cpq *CharacterProficiencyQuery) Order(o ...characterproficiency.OrderOption) *CharacterProficiencyQuery {
	cpq.order = append(cpq.order, o...)
	return cpq
}

// QueryCharacter chains the current query on the "character" edge.
func (cpq *CharacterProficiencyQuery) QueryCharacter() *CharacterQuery {
	query := (&CharacterClient{config: cpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(characterproficiency.Table, characterproficiency.FieldID, selector),
			sqlgraph.To(character.Table, character.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, characterproficiency.CharacterTable, characterproficiency.CharacterColumn),
		)
		fromU = sqlgraph.SetNeighbors(cpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryProficiency chains the current query on the "proficiency" edge.
func (cpq *CharacterProficiencyQuery) QueryProficiency() *ProficiencyQuery {
	query := (&ProficiencyClient{config: cpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(characterproficiency.Table, characterproficiency.FieldID, selector),
			sqlgraph.To(proficiency.Table, proficiency.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, characterproficiency.ProficiencyTable, characterproficiency.ProficiencyColumn),
		)
		fromU = sqlgraph.SetNeighbors(cpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCharacterSkill chains the current query on the "character_skill" edge.
func (cpq *CharacterProficiencyQuery) QueryCharacterSkill() *CharacterSkillQuery {
	query := (&CharacterSkillClient{config: cpq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cpq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cpq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(characterproficiency.Table, characterproficiency.FieldID, selector),
			sqlgraph.To(characterskill.Table, characterskill.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, characterproficiency.CharacterSkillTable, characterproficiency.CharacterSkillColumn),
		)
		fromU = sqlgraph.SetNeighbors(cpq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CharacterProficiency entity from the query.
// Returns a *NotFoundError when no CharacterProficiency was found.
func (cpq *CharacterProficiencyQuery) First(ctx context.Context) (*CharacterProficiency, error) {
	nodes, err := cpq.Limit(1).All(setContextOp(ctx, cpq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{characterproficiency.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) FirstX(ctx context.Context) *CharacterProficiency {
	node, err := cpq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CharacterProficiency ID from the query.
// Returns a *NotFoundError when no CharacterProficiency ID was found.
func (cpq *CharacterProficiencyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cpq.Limit(1).IDs(setContextOp(ctx, cpq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{characterproficiency.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) FirstIDX(ctx context.Context) int {
	id, err := cpq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CharacterProficiency entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CharacterProficiency entity is found.
// Returns a *NotFoundError when no CharacterProficiency entities are found.
func (cpq *CharacterProficiencyQuery) Only(ctx context.Context) (*CharacterProficiency, error) {
	nodes, err := cpq.Limit(2).All(setContextOp(ctx, cpq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{characterproficiency.Label}
	default:
		return nil, &NotSingularError{characterproficiency.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) OnlyX(ctx context.Context) *CharacterProficiency {
	node, err := cpq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CharacterProficiency ID in the query.
// Returns a *NotSingularError when more than one CharacterProficiency ID is found.
// Returns a *NotFoundError when no entities are found.
func (cpq *CharacterProficiencyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cpq.Limit(2).IDs(setContextOp(ctx, cpq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{characterproficiency.Label}
	default:
		err = &NotSingularError{characterproficiency.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) OnlyIDX(ctx context.Context) int {
	id, err := cpq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CharacterProficiencies.
func (cpq *CharacterProficiencyQuery) All(ctx context.Context) ([]*CharacterProficiency, error) {
	ctx = setContextOp(ctx, cpq.ctx, ent.OpQueryAll)
	if err := cpq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CharacterProficiency, *CharacterProficiencyQuery]()
	return withInterceptors[[]*CharacterProficiency](ctx, cpq, qr, cpq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) AllX(ctx context.Context) []*CharacterProficiency {
	nodes, err := cpq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CharacterProficiency IDs.
func (cpq *CharacterProficiencyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if cpq.ctx.Unique == nil && cpq.path != nil {
		cpq.Unique(true)
	}
	ctx = setContextOp(ctx, cpq.ctx, ent.OpQueryIDs)
	if err = cpq.Select(characterproficiency.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) IDsX(ctx context.Context) []int {
	ids, err := cpq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cpq *CharacterProficiencyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cpq.ctx, ent.OpQueryCount)
	if err := cpq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cpq, querierCount[*CharacterProficiencyQuery](), cpq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) CountX(ctx context.Context) int {
	count, err := cpq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cpq *CharacterProficiencyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cpq.ctx, ent.OpQueryExist)
	switch _, err := cpq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cpq *CharacterProficiencyQuery) ExistX(ctx context.Context) bool {
	exist, err := cpq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CharacterProficiencyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cpq *CharacterProficiencyQuery) Clone() *CharacterProficiencyQuery {
	if cpq == nil {
		return nil
	}
	return &CharacterProficiencyQuery{
		config:             cpq.config,
		ctx:                cpq.ctx.Clone(),
		order:              append([]characterproficiency.OrderOption{}, cpq.order...),
		inters:             append([]Interceptor{}, cpq.inters...),
		predicates:         append([]predicate.CharacterProficiency{}, cpq.predicates...),
		withCharacter:      cpq.withCharacter.Clone(),
		withProficiency:    cpq.withProficiency.Clone(),
		withCharacterSkill: cpq.withCharacterSkill.Clone(),
		// clone intermediate query.
		sql:  cpq.sql.Clone(),
		path: cpq.path,
	}
}

// WithCharacter tells the query-builder to eager-load the nodes that are connected to
// the "character" edge. The optional arguments are used to configure the query builder of the edge.
func (cpq *CharacterProficiencyQuery) WithCharacter(opts ...func(*CharacterQuery)) *CharacterProficiencyQuery {
	query := (&CharacterClient{config: cpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cpq.withCharacter = query
	return cpq
}

// WithProficiency tells the query-builder to eager-load the nodes that are connected to
// the "proficiency" edge. The optional arguments are used to configure the query builder of the edge.
func (cpq *CharacterProficiencyQuery) WithProficiency(opts ...func(*ProficiencyQuery)) *CharacterProficiencyQuery {
	query := (&ProficiencyClient{config: cpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cpq.withProficiency = query
	return cpq
}

// WithCharacterSkill tells the query-builder to eager-load the nodes that are connected to
// the "character_skill" edge. The optional arguments are used to configure the query builder of the edge.
func (cpq *CharacterProficiencyQuery) WithCharacterSkill(opts ...func(*CharacterSkillQuery)) *CharacterProficiencyQuery {
	query := (&CharacterSkillClient{config: cpq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cpq.withCharacterSkill = query
	return cpq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ProficiencyType characterproficiency.ProficiencyType `json:"proficiency_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CharacterProficiency.Query().
//		GroupBy(characterproficiency.FieldProficiencyType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cpq *CharacterProficiencyQuery) GroupBy(field string, fields ...string) *CharacterProficiencyGroupBy {
	cpq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CharacterProficiencyGroupBy{build: cpq}
	grbuild.flds = &cpq.ctx.Fields
	grbuild.label = characterproficiency.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ProficiencyType characterproficiency.ProficiencyType `json:"proficiency_type,omitempty"`
//	}
//
//	client.CharacterProficiency.Query().
//		Select(characterproficiency.FieldProficiencyType).
//		Scan(ctx, &v)
func (cpq *CharacterProficiencyQuery) Select(fields ...string) *CharacterProficiencySelect {
	cpq.ctx.Fields = append(cpq.ctx.Fields, fields...)
	sbuild := &CharacterProficiencySelect{CharacterProficiencyQuery: cpq}
	sbuild.label = characterproficiency.Label
	sbuild.flds, sbuild.scan = &cpq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CharacterProficiencySelect configured with the given aggregations.
func (cpq *CharacterProficiencyQuery) Aggregate(fns ...AggregateFunc) *CharacterProficiencySelect {
	return cpq.Select().Aggregate(fns...)
}

func (cpq *CharacterProficiencyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cpq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cpq); err != nil {
				return err
			}
		}
	}
	for _, f := range cpq.ctx.Fields {
		if !characterproficiency.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cpq.path != nil {
		prev, err := cpq.path(ctx)
		if err != nil {
			return err
		}
		cpq.sql = prev
	}
	return nil
}

func (cpq *CharacterProficiencyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CharacterProficiency, error) {
	var (
		nodes       = []*CharacterProficiency{}
		withFKs     = cpq.withFKs
		_spec       = cpq.querySpec()
		loadedTypes = [3]bool{
			cpq.withCharacter != nil,
			cpq.withProficiency != nil,
			cpq.withCharacterSkill != nil,
		}
	)
	if cpq.withCharacter != nil || cpq.withProficiency != nil || cpq.withCharacterSkill != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, characterproficiency.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CharacterProficiency).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CharacterProficiency{config: cpq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(cpq.modifiers) > 0 {
		_spec.Modifiers = cpq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cpq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cpq.withCharacter; query != nil {
		if err := cpq.loadCharacter(ctx, query, nodes, nil,
			func(n *CharacterProficiency, e *Character) { n.Edges.Character = e }); err != nil {
			return nil, err
		}
	}
	if query := cpq.withProficiency; query != nil {
		if err := cpq.loadProficiency(ctx, query, nodes, nil,
			func(n *CharacterProficiency, e *Proficiency) { n.Edges.Proficiency = e }); err != nil {
			return nil, err
		}
	}
	if query := cpq.withCharacterSkill; query != nil {
		if err := cpq.loadCharacterSkill(ctx, query, nodes, nil,
			func(n *CharacterProficiency, e *CharacterSkill) { n.Edges.CharacterSkill = e }); err != nil {
			return nil, err
		}
	}
	for i := range cpq.loadTotal {
		if err := cpq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cpq *CharacterProficiencyQuery) loadCharacter(ctx context.Context, query *CharacterQuery, nodes []*CharacterProficiency, init func(*CharacterProficiency), assign func(*CharacterProficiency, *Character)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CharacterProficiency)
	for i := range nodes {
		if nodes[i].character_character_proficiencies == nil {
			continue
		}
		fk := *nodes[i].character_character_proficiencies
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(character.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "character_character_proficiencies" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (cpq *CharacterProficiencyQuery) loadProficiency(ctx context.Context, query *ProficiencyQuery, nodes []*CharacterProficiency, init func(*CharacterProficiency), assign func(*CharacterProficiency, *Proficiency)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CharacterProficiency)
	for i := range nodes {
		if nodes[i].character_proficiency_proficiency == nil {
			continue
		}
		fk := *nodes[i].character_proficiency_proficiency
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(proficiency.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "character_proficiency_proficiency" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (cpq *CharacterProficiencyQuery) loadCharacterSkill(ctx context.Context, query *CharacterSkillQuery, nodes []*CharacterProficiency, init func(*CharacterProficiency), assign func(*CharacterProficiency, *CharacterSkill)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*CharacterProficiency)
	for i := range nodes {
		if nodes[i].character_skill_character_proficiency == nil {
			continue
		}
		fk := *nodes[i].character_skill_character_proficiency
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(characterskill.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "character_skill_character_proficiency" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (cpq *CharacterProficiencyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cpq.querySpec()
	if len(cpq.modifiers) > 0 {
		_spec.Modifiers = cpq.modifiers
	}
	_spec.Node.Columns = cpq.ctx.Fields
	if len(cpq.ctx.Fields) > 0 {
		_spec.Unique = cpq.ctx.Unique != nil && *cpq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cpq.driver, _spec)
}

func (cpq *CharacterProficiencyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(characterproficiency.Table, characterproficiency.Columns, sqlgraph.NewFieldSpec(characterproficiency.FieldID, field.TypeInt))
	_spec.From = cpq.sql
	if unique := cpq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cpq.path != nil {
		_spec.Unique = true
	}
	if fields := cpq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, characterproficiency.FieldID)
		for i := range fields {
			if fields[i] != characterproficiency.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cpq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cpq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cpq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cpq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cpq *CharacterProficiencyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cpq.driver.Dialect())
	t1 := builder.Table(characterproficiency.Table)
	columns := cpq.ctx.Fields
	if len(columns) == 0 {
		columns = characterproficiency.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cpq.sql != nil {
		selector = cpq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cpq.ctx.Unique != nil && *cpq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cpq.predicates {
		p(selector)
	}
	for _, p := range cpq.order {
		p(selector)
	}
	if offset := cpq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cpq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CharacterProficiencyGroupBy is the group-by builder for CharacterProficiency entities.
type CharacterProficiencyGroupBy struct {
	selector
	build *CharacterProficiencyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cpgb *CharacterProficiencyGroupBy) Aggregate(fns ...AggregateFunc) *CharacterProficiencyGroupBy {
	cpgb.fns = append(cpgb.fns, fns...)
	return cpgb
}

// Scan applies the selector query and scans the result into the given value.
func (cpgb *CharacterProficiencyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cpgb.build.ctx, ent.OpQueryGroupBy)
	if err := cpgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CharacterProficiencyQuery, *CharacterProficiencyGroupBy](ctx, cpgb.build, cpgb, cpgb.build.inters, v)
}

func (cpgb *CharacterProficiencyGroupBy) sqlScan(ctx context.Context, root *CharacterProficiencyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cpgb.fns))
	for _, fn := range cpgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cpgb.flds)+len(cpgb.fns))
		for _, f := range *cpgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cpgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cpgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CharacterProficiencySelect is the builder for selecting fields of CharacterProficiency entities.
type CharacterProficiencySelect struct {
	*CharacterProficiencyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cps *CharacterProficiencySelect) Aggregate(fns ...AggregateFunc) *CharacterProficiencySelect {
	cps.fns = append(cps.fns, fns...)
	return cps
}

// Scan applies the selector query and scans the result into the given value.
func (cps *CharacterProficiencySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cps.ctx, ent.OpQuerySelect)
	if err := cps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CharacterProficiencyQuery, *CharacterProficiencySelect](ctx, cps.CharacterProficiencyQuery, cps, cps.inters, v)
}

func (cps *CharacterProficiencySelect) sqlScan(ctx context.Context, root *CharacterProficiencyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cps.fns))
	for _, fn := range cps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
