// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ecshreve/dndgen/ent/coin"
	"github.com/ecshreve/dndgen/ent/cost"
	"github.com/ecshreve/dndgen/ent/equipment"
)

// Cost is the model entity for the Cost schema.
type Cost struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity int `json:"quantity,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CostQuery when eager-loading is set.
	Edges          CostEdges `json:"-"`
	cost_coin      *int
	equipment_cost *int
	selectValues   sql.SelectValues
}

// CostEdges holds the relations/edges for other nodes in the graph.
type CostEdges struct {
	// Coin holds the value of the coin edge.
	Coin *Coin `json:"coin,omitempty"`
	// Equipment holds the value of the equipment edge.
	Equipment *Equipment `json:"equipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// CoinOrErr returns the Coin value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CostEdges) CoinOrErr() (*Coin, error) {
	if e.Coin != nil {
		return e.Coin, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: coin.Label}
	}
	return nil, &NotLoadedError{edge: "coin"}
}

// EquipmentOrErr returns the Equipment value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CostEdges) EquipmentOrErr() (*Equipment, error) {
	if e.Equipment != nil {
		return e.Equipment, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: equipment.Label}
	}
	return nil, &NotLoadedError{edge: "equipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Cost) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case cost.FieldID, cost.FieldQuantity:
			values[i] = new(sql.NullInt64)
		case cost.ForeignKeys[0]: // cost_coin
			values[i] = new(sql.NullInt64)
		case cost.ForeignKeys[1]: // equipment_cost
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Cost fields.
func (c *Cost) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case cost.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			c.ID = int(value.Int64)
		case cost.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				c.Quantity = int(value.Int64)
			}
		case cost.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field cost_coin", value)
			} else if value.Valid {
				c.cost_coin = new(int)
				*c.cost_coin = int(value.Int64)
			}
		case cost.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field equipment_cost", value)
			} else if value.Valid {
				c.equipment_cost = new(int)
				*c.equipment_cost = int(value.Int64)
			}
		default:
			c.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Cost.
// This includes values selected through modifiers, order, etc.
func (c *Cost) Value(name string) (ent.Value, error) {
	return c.selectValues.Get(name)
}

// QueryCoin queries the "coin" edge of the Cost entity.
func (c *Cost) QueryCoin() *CoinQuery {
	return NewCostClient(c.config).QueryCoin(c)
}

// QueryEquipment queries the "equipment" edge of the Cost entity.
func (c *Cost) QueryEquipment() *EquipmentQuery {
	return NewCostClient(c.config).QueryEquipment(c)
}

// Update returns a builder for updating this Cost.
// Note that you need to call Cost.Unwrap() before calling this method if this Cost
// was returned from a transaction, and the transaction was committed or rolled back.
func (c *Cost) Update() *CostUpdateOne {
	return NewCostClient(c.config).UpdateOne(c)
}

// Unwrap unwraps the Cost entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (c *Cost) Unwrap() *Cost {
	_tx, ok := c.config.driver.(*txDriver)
	if !ok {
		panic("ent: Cost is not a transactional entity")
	}
	c.config.driver = _tx.drv
	return c
}

// String implements the fmt.Stringer.
func (c *Cost) String() string {
	var builder strings.Builder
	builder.WriteString("Cost(")
	builder.WriteString(fmt.Sprintf("id=%v, ", c.ID))
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", c.Quantity))
	builder.WriteByte(')')
	return builder.String()
}

// MarshalJSON implements the json.Marshaler interface.
func (c *Cost) MarshalJSON() ([]byte, error) {
	type Alias Cost
	return json.Marshal(&struct {
		*Alias
		CostEdges
	}{
		Alias:     (*Alias)(c),
		CostEdges: c.Edges,
	})
}

// UnmarshalJSON implements the json.Unmarshaler interface.
func (c *Cost) UnmarshalJSON(data []byte) error {
	type Alias Cost
	aux := &struct {
		*Alias
		CostEdges
	}{
		Alias: (*Alias)(c),
	}

	if err := json.Unmarshal(data, &aux); err != nil {
		return err
	}

	c.Edges = aux.CostEdges
	return nil
}

func (cc *CostCreate) SetCost(input *Cost) *CostCreate {
	cc.SetQuantity(input.Quantity)
	return cc
}

// Costs is a parsable slice of Cost.
type Costs []*Cost
