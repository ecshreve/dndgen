// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ecshreve/dndgen/ent/alignment"
)

// Alignment is the model entity for the Alignment schema.
type Alignment struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Indx holds the value of the "indx" field.
	Indx string `json:"index"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Desc holds the value of the "desc" field.
	Desc string `json:"desc,omitempty"`
	// Abbr holds the value of the "abbr" field.
	Abbr         string `json:"abbr,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Alignment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case alignment.FieldID:
			values[i] = new(sql.NullInt64)
		case alignment.FieldIndx, alignment.FieldName, alignment.FieldDesc, alignment.FieldAbbr:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Alignment fields.
func (a *Alignment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case alignment.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			a.ID = int(value.Int64)
		case alignment.FieldIndx:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field indx", values[i])
			} else if value.Valid {
				a.Indx = value.String
			}
		case alignment.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				a.Name = value.String
			}
		case alignment.FieldDesc:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field desc", values[i])
			} else if value.Valid {
				a.Desc = value.String
			}
		case alignment.FieldAbbr:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field abbr", values[i])
			} else if value.Valid {
				a.Abbr = value.String
			}
		default:
			a.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Alignment.
// This includes values selected through modifiers, order, etc.
func (a *Alignment) Value(name string) (ent.Value, error) {
	return a.selectValues.Get(name)
}

// Update returns a builder for updating this Alignment.
// Note that you need to call Alignment.Unwrap() before calling this method if this Alignment
// was returned from a transaction, and the transaction was committed or rolled back.
func (a *Alignment) Update() *AlignmentUpdateOne {
	return NewAlignmentClient(a.config).UpdateOne(a)
}

// Unwrap unwraps the Alignment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (a *Alignment) Unwrap() *Alignment {
	_tx, ok := a.config.driver.(*txDriver)
	if !ok {
		panic("ent: Alignment is not a transactional entity")
	}
	a.config.driver = _tx.drv
	return a
}

// String implements the fmt.Stringer.
func (a *Alignment) String() string {
	var builder strings.Builder
	builder.WriteString("Alignment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", a.ID))
	builder.WriteString("indx=")
	builder.WriteString(a.Indx)
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(a.Name)
	builder.WriteString(", ")
	builder.WriteString("desc=")
	builder.WriteString(a.Desc)
	builder.WriteString(", ")
	builder.WriteString("abbr=")
	builder.WriteString(a.Abbr)
	builder.WriteByte(')')
	return builder.String()
}

// Alignments is a parsable slice of Alignment.
type Alignments []*Alignment
