// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/ecshreve/dndgen/ent/magicitem"
)

// MagicItem is the model entity for the MagicItem schema.
type MagicItem struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Rarity holds the value of the "rarity" field.
	Rarity string `json:"rarity,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MagicItemQuery when eager-loading is set.
	Edges        MagicItemEdges `json:"edges"`
	selectValues sql.SelectValues
}

// MagicItemEdges holds the relations/edges for other nodes in the graph.
type MagicItemEdges struct {
	// Equipment holds the value of the equipment edge.
	Equipment []*Equipment `json:"equipment,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedEquipment map[string][]*Equipment
}

// EquipmentOrErr returns the Equipment value or an error if the edge
// was not loaded in eager-loading.
func (e MagicItemEdges) EquipmentOrErr() ([]*Equipment, error) {
	if e.loadedTypes[0] {
		return e.Equipment, nil
	}
	return nil, &NotLoadedError{edge: "equipment"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MagicItem) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case magicitem.FieldID:
			values[i] = new(sql.NullInt64)
		case magicitem.FieldRarity:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MagicItem fields.
func (mi *MagicItem) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case magicitem.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			mi.ID = int(value.Int64)
		case magicitem.FieldRarity:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rarity", values[i])
			} else if value.Valid {
				mi.Rarity = value.String
			}
		default:
			mi.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MagicItem.
// This includes values selected through modifiers, order, etc.
func (mi *MagicItem) Value(name string) (ent.Value, error) {
	return mi.selectValues.Get(name)
}

// QueryEquipment queries the "equipment" edge of the MagicItem entity.
func (mi *MagicItem) QueryEquipment() *EquipmentQuery {
	return NewMagicItemClient(mi.config).QueryEquipment(mi)
}

// Update returns a builder for updating this MagicItem.
// Note that you need to call MagicItem.Unwrap() before calling this method if this MagicItem
// was returned from a transaction, and the transaction was committed or rolled back.
func (mi *MagicItem) Update() *MagicItemUpdateOne {
	return NewMagicItemClient(mi.config).UpdateOne(mi)
}

// Unwrap unwraps the MagicItem entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mi *MagicItem) Unwrap() *MagicItem {
	_tx, ok := mi.config.driver.(*txDriver)
	if !ok {
		panic("ent: MagicItem is not a transactional entity")
	}
	mi.config.driver = _tx.drv
	return mi
}

// String implements the fmt.Stringer.
func (mi *MagicItem) String() string {
	var builder strings.Builder
	builder.WriteString("MagicItem(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mi.ID))
	builder.WriteString("rarity=")
	builder.WriteString(mi.Rarity)
	builder.WriteByte(')')
	return builder.String()
}

func (mic *MagicItemCreate) SetMagicItem(input *MagicItem) *MagicItemCreate {
	mic.SetRarity(input.Rarity)
	return mic
}

// NamedEquipment returns the Equipment named value or an error if the edge was not
// loaded in eager-loading with this name.
func (mi *MagicItem) NamedEquipment(name string) ([]*Equipment, error) {
	if mi.Edges.namedEquipment == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := mi.Edges.namedEquipment[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (mi *MagicItem) appendNamedEquipment(name string, edges ...*Equipment) {
	if mi.Edges.namedEquipment == nil {
		mi.Edges.namedEquipment = make(map[string][]*Equipment)
	}
	if len(edges) == 0 {
		mi.Edges.namedEquipment[name] = []*Equipment{}
	} else {
		mi.Edges.namedEquipment[name] = append(mi.Edges.namedEquipment[name], edges...)
	}
}

// MagicItems is a parsable slice of MagicItem.
type MagicItems []*MagicItem
