// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ecshreve/dndgen/ent/equipment"
	"github.com/ecshreve/dndgen/ent/pack"
)

// PackCreate is the builder for creating a Pack entity.
type PackCreate struct {
	config
	mutation *PackMutation
	hooks    []Hook
}

// SetContents sets the "contents" field.
func (pc *PackCreate) SetContents(s string) *PackCreate {
	pc.mutation.SetContents(s)
	return pc
}

// AddEquipmentIDs adds the "equipment" edge to the Equipment entity by IDs.
func (pc *PackCreate) AddEquipmentIDs(ids ...int) *PackCreate {
	pc.mutation.AddEquipmentIDs(ids...)
	return pc
}

// AddEquipment adds the "equipment" edges to the Equipment entity.
func (pc *PackCreate) AddEquipment(e ...*Equipment) *PackCreate {
	ids := make([]int, len(e))
	for i := range e {
		ids[i] = e[i].ID
	}
	return pc.AddEquipmentIDs(ids...)
}

// Mutation returns the PackMutation object of the builder.
func (pc *PackCreate) Mutation() *PackMutation {
	return pc.mutation
}

// Save creates the Pack in the database.
func (pc *PackCreate) Save(ctx context.Context) (*Pack, error) {
	return withHooks[*Pack, PackMutation](ctx, pc.sqlSave, pc.mutation, pc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PackCreate) SaveX(ctx context.Context) *Pack {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PackCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PackCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PackCreate) check() error {
	if _, ok := pc.mutation.Contents(); !ok {
		return &ValidationError{Name: "contents", err: errors.New(`ent: missing required field "Pack.contents"`)}
	}
	return nil
}

func (pc *PackCreate) sqlSave(ctx context.Context) (*Pack, error) {
	if err := pc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	pc.mutation.id = &_node.ID
	pc.mutation.done = true
	return _node, nil
}

func (pc *PackCreate) createSpec() (*Pack, *sqlgraph.CreateSpec) {
	var (
		_node = &Pack{config: pc.config}
		_spec = sqlgraph.NewCreateSpec(pack.Table, sqlgraph.NewFieldSpec(pack.FieldID, field.TypeInt))
	)
	if value, ok := pc.mutation.Contents(); ok {
		_spec.SetField(pack.FieldContents, field.TypeString, value)
		_node.Contents = value
	}
	if nodes := pc.mutation.EquipmentIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2M,
			Inverse: true,
			Table:   pack.EquipmentTable,
			Columns: pack.EquipmentPrimaryKey,
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(equipment.FieldID, field.TypeInt),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PackCreateBulk is the builder for creating many Pack entities in bulk.
type PackCreateBulk struct {
	config
	builders []*PackCreate
}

// Save creates the Pack entities in the database.
func (pcb *PackCreateBulk) Save(ctx context.Context) ([]*Pack, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Pack, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PackMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PackCreateBulk) SaveX(ctx context.Context) []*Pack {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PackCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PackCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}
